{
  "name": "docs-cms",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces foreach run build",
    "yalc": "yarn workspaces foreach run yalc"
  },
  "version": "1.0.0",
  "devDependencies": {
    "@types/jest": "^26.0.23",
    "@types/node": "^15.12.2"
  }
}
